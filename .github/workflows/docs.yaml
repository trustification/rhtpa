name: docs

on:
  push:
    branches:
      - "main"
  workflow_dispatch:

concurrency: docs

jobs:

  build:
    runs-on: ubuntu-24.04

    steps:

      - name: Checkout
        uses: actions/checkout@v7

      - run: |
          cd docs/book
          npm ci
          make all
