services:
  s3mock:
    image: adobe/s3mock:latest
    ports:
      - "9090:9090"
    healthcheck:
      test: ["CMD-SHELL", "wget -q --spider http://localhost:9090/ || exit 1"]
      interval: 5s
      timeout: 5s
      retries: 5
